/*
// ## Program-1 map normal map function
const myNum=[1,2,3,4,5]
const numbers=myNum.map((nums)=>nums +10)
console.log(numbers);   //output:[ 11, 12, 13, 14, 15 ]
*/

/*
 //## Program-2 If I apply CONDTION ON map it will output true or false
const myNum=[1,2,3,4,5]
const numbers=myNum.map((nums)=>nums >3)
console.log(numbers); //output:[ false, false, false, true, true ]

*/

 /*
//## Program-3 map chaining in map we can apply n numbers of .map chaining
const myNum = [1, 2, 3, 4, 5, 6, 7];
const numbers = myNum
  .map((nums) => nums * 10)
  .map((nums) => nums + 10)
  .filter((nums) => nums > 50);
console.log(numbers);            //output:[ 60, 70, 80 ]

 */